[pytest]
testpaths = tests
addopts = -m "not slow" --strict-markers -q
markers =
    slow: parameter sweeps measured in minutes. Excluded by default; run
        them deliberately with `pytest -m slow`, or everything with
        `pytest -m "slow or not slow"`.
